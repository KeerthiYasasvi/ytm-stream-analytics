name: Support Concierge Bot

on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write

jobs:
  supportbot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
 
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Run Support Concierge
        if: github.event_name == 'issues' || github.event.comment.user.login != 'github-actions[bot]'
        run: dotnet run --project bot/src/SupportConcierge.Cli -- --event-file "$GITHUB_EVENT_PATH"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SUPPORTBOT_USERNAME: github-actions[bot]
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY || secrets.API_KEY }}
          OPENAI_MODEL: ${{ vars.PRIMARY_MODEL || 'gpt-5.4-nano' }}
          OPENAI_CRITIQUE_MODEL: ${{ vars.SECONDARY_MODEL || 'gpt-5-nano' }}
          SUPPORTBOT_DRY_RUN: 'false'
          SUPPORTBOT_WRITE_MODE: 'true'
          SUPPORTBOT_SPEC_DIR: '.supportbot'
          SUPPORTBOT_RUBRICS_DIR: evals/rubrics
          SUPPORTBOT_EVALS_DIR: artifacts/evals
          SUPPORTBOT_METRICS_DIR: artifacts/metrics
          SUPPORTBOT_TELEMETRY_DIR: artifacts/telemetry

      - name: Upload metrics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: supportbot-metrics
          path: artifacts/metrics

      - name: Upload telemetry
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: supportbot-telemetry
          path: artifacts/telemetry

      - name: Upload evals
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: supportbot-evals
          path: artifacts/evals
